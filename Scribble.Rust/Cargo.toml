[package]
name = "scribble-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.31"
tiny-skia = "0.11"

[build-dependencies]

# Link to WintabSession.dll (pen_session C API).
# The DLL must be in the same directory as the exe at runtime.
